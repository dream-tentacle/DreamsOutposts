这个项目的目标，是做一个独立于 Vanilla Expanded Framework 的 RimWorld 世界地图据点系统。它最直接的用途当然是填补“不想装 VEF，但又想要据点玩法”的空缺，但如果只是把 Vanilla Outposts Expanded 重新实现一遍，意义其实不大。更希望做的是，在保留“派几个 Pawn 去世界地图建立据点”这种简单直觉的同时，让据点真正成为一种长期存在、能够成长、积累资源、承担风险并产生故事的世界资产。

这里需要刻意避免另一个极端，也就是 Empire 那种接近第二套大战略游戏的方向。我们并不想模拟国家、领土、税收、军队和复杂物流。玩家的主基地仍然应该是游戏中心。世界地图上的据点负责扩展殖民地的触角，但重要的收益、事件和风险最终仍然应该和 RimWorld 原本的殖民地玩法发生联系。

据点本身不是第二张持续运行的地图。里面驻扎的 Pawn 仍然是真实的 RimWorld Pawn，而不是几个抽象人口数字，但不会继续模拟他们每分钟在做什么工作。生产、治疗、防御等行为都应通过周期性的抽象结算完成。这样玩家能够真正把自己的殖民者派出去，又不需要承受多殖民地那种大量 Job、地图 Tick 和管理负担。

最基础的据点可以从一个矿业据点开始。玩家把一个或几个 Pawn 派到世界地图上的某个位置，这个位置生成一个长期存在的据点。系统根据这些 Pawn 的 Mining 技能定期计算产量，并把产生的 Steel 存进据点自己的库存。这里“库存”是一个很重要的设计区别：资源不会像传统据点 Mod 那样隔一段时间凭空送进主基地，而是真正积累在那个地方。玩家打开据点界面时，应当能看到里面驻扎了谁、当前库存有什么、最近的生产情况如何。

玩家取得这些资源时，不准备引入道路或者复杂运输线路。世界地图物流很容易变成一个独立的大系统，而且对大多数玩家来说可能只是额外负担。基础方案只保留两个很容易理解的办法：玩家自己派 Caravan 过去取货，或者让据点把物资空投回主基地。Caravan 不需要额外费用，但需要实际旅行；空投则方便得多，不过需要消耗一些 Steel、Components 或类似资源制作运输舱。运输成本更适合作为额外支出，而不是直接从货物中扣掉百分比，这样玩家也更容易理解“我运 500 Steel，就会收到 500 Steel，只是另外花了发射成本”。

在这个基础上，据点以后会逐渐拥有等级和设施。等级不是简单的“Lv2 产量 +20%”，而应该表示这个地方已经从临时营地逐渐发展成正式设施。等级可以决定允许驻扎的人数、库存规模、设施槽数量以及能够遇到和处理的事件。具体升级条件暂时没有必要现在锁死，可以以后结合运营时间、投入资源、驻扎人员、累计产量或者特殊事件再决定。

设施是据点成长的主要选择来源。这里不希望变成一个小型城市建造游戏，所以一个高级据点仍然只需要少量几个真正重要的设施槽。比如矿业据点可以选择强化采掘、扩充仓库，也可以选择建设武器库和防御设施。如果所有据点最后都是把同样的六个建筑全部点满，那么这个系统实际上就没有选择。因此设施更应该推动据点形成不同倾向，而不是单纯提供一条固定升级路线。

防御系统也不准备模拟一场发生在抽象据点里的真实 RimWorld 战斗。驻扎 Pawn、武器、护甲和设施会共同产生一个玩家容易理解的防御能力。武器方面可以自动读取游戏中已有的武器数据，例如 DPS、射程或其他必要参数，再转换成据点战斗贡献。这个数值不需要严格预测一场真实遭遇战的结果。RimWorld 里不同武器的真实战斗价值受到很多因素影响，如果为了完全准确而重新实现一套战斗模拟，成本和复杂度都会迅速失控。这里更重要的是玩家能够很自然地理解：“我往这个据点送更好的枪，它就会更安全。”

据点受到威胁以后，也不希望频繁要求玩家跨地图跑过去救援。那种设计在只有一个据点时可能有趣，但玩家建立五六个以后就会变成不断救火。更符合 RimWorld 本身节奏的方案，是让防御不足的外围据点增加整个殖民地面临的风险。比如一个高价值、低防御的矿场被海盗发现以后，可以弹出通知告诉玩家某个敌对势力正在准备袭击，几天之后真正的 Raid 则发生在玩家主基地。这个袭击仍然使用 RimWorld 原本成熟的战斗系统，而据点的价值和防御能力只是参与计算袭击倍率。这样世界地图扩张确实会带来新的风险，但最终玩家仍然是在自己最熟悉的殖民地里处理这些风险。

以后也可以存在真正针对据点本身的危险，但那应该更像长期危机，而不是突然删除据点。例如敌人开始围困一个据点，玩家有较长时间补充武器、投入资源或者做出其他选择。如果最终失守，据点才可能受到严重损坏甚至被摧毁。这里的具体形式可以等真正开始做防御系统的时候再决定。

随机事件是后期非常重要的一部分。希望它更接近 Frostpunk 那种“事件本身是一个决策”，而不是普通的随机 Buff 或 Debuff。比如一批难民到达据点，普通据点可能只能选择接纳或拒绝。如果这个据点具有足够强的军事能力，就会出现“扣押他们”这样的额外选项，并可能把其中一部分 Pawn 变成 RimWorld 原本的 Slave 身份。医疗能力强的据点可能有救治相关选项，Social 或外交能力高的据点可能可以招募或者谈判。

这意味着设施、驻扎 Pawn 和装备带来的意义不能只体现在“产量 +15%”上。它们还应该改变据点能够怎样处理世界事件。一个强大的据点不是单纯数字更高，而是玩家在事件发生时拥有更多手段。

为了支持这种设计，据点后期可能会拥有 Defense、Medical、Research、Production、Storage、Social 等能力值。但这些能力不应该写成 C# enum。更合适的方向是让能力本身也是 Def，例如 `OutpostStatDef`。这样核心 Mod 可以定义 Defense、Medical 等基础能力，而其他 Mod 完全可以再添加自己的 `MagicPower`、`PsychicControl`、`GeneticResearch` 或 `AnomalyContainment`。核心代码只负责“查询某个 OutpostStatDef 当前是多少”，而不需要知道世界上到底存在多少种能力。

这个设计也是整个项目拓展性的核心。

我们希望第三方 Mod 接入时，并不是先研究整个据点框架的内部结构，然后重写一套据点逻辑，而是能够直接在已有系统上增加内容。最常见的扩展应该尽量只是定义新的数据。例如一个 Mod 想加入“魔导能源设施”，它只需要定义一个设施 Def，并说明这个设施给某个 `OutpostStatDef` 增加多少数值。如果它想加入一种新的“灵能研究据点”，应该主要通过定义新的 `OutpostDef`、生产规则、允许使用的设施和事件来完成。

也就是说，默认情况应该是“配置现有机制”，而不是“继承核心对象”。

这里可以把扩展分成两个层级理解。

第一层是内容扩展。第三方作者只是想添加新的据点、设施、能力或者事件。这种情况下最好主要写 Def/XML。他不需要关心据点怎样保存 Pawn、怎样做存档、怎样显示 UI、怎样进行 Tick 结算，也不需要重新实现库存和运输。所有这些通用功能都应该由核心框架负责。

第二层才是行为扩展。如果某个 Mod 的规则真的超出了已有配置能力，例如一个捕鲸据点只能建立在海岸、冬季产量不同，或者一个特殊设施的效率取决于 Pawn 的 PsychicSensitivity，这时候作者才需要引用我们的程序集并继承一个 Worker。

Worker 的定位应该非常明确：它不是一个完整据点类，而只是处理“这个内容有哪些特殊行为”。

例如 `OutpostDef` 可以拥有一个可选的 `workerClass`。正常矿业据点完全不需要自己的 Worker，直接使用默认实现即可。如果第三方真的有特殊逻辑，它可以继承 `OutpostWorker`，只 override 自己需要改变的部分，例如 `CanCreate` 或某个生产计算方法。它不应该因此需要重新负责 `ExposeData`、WorldObject 生命周期、库存、Gizmo 或 UI 等通用部分。

同样的思路以后也可以用于设施和事件。简单设施只提供 Stat Offset，不需要代码；特殊设施才拥有 `FacilityWorker`。大多数事件可以由一组通用的 Requirement 和 Effect 组合出来，例如“Defense 至少 120”“拥有医疗设施”“移除 50 Medicine”“生成两个 Slave”“降低某派系 Goodwill”。只有真正无法表达的事件条件或者效果才需要第三方写自己的 Requirement 或 Effect 类。

这种架构的目的并不是追求“不让别人引用 DLL”。引用 DLL 完全没有问题。真正需要降低的是接入工作量。一个作者只想给自己的武器或者科技增加一点据点联动时，不应该被迫学习完整 API；只有他真的想创造新行为的时候，才逐渐进入 C# 扩展层。

武器是一个很好的自动兼容例子。核心系统应该默认认识所有符合 RimWorld 正常武器定义的 `ThingDef`，根据已有数据自动计算其防御贡献。这样某个武器 Mod 即使完全不知道我们的据点系统存在，玩家仍然可以把它的武器送入据点并获得合理的战斗力。如果作者觉得自己的某种特殊武器被自动公式严重高估或低估，再通过 `DefModExtension` 或类似机制提供一个修正倍率即可。这样的兼容方式比要求每个武器作者手动注册要轻得多。

同样，我们自己的内容也应该使用和第三方完全相同的扩展方式。如果核心 Mod 的矿场是通过硬编码生成，而第三方必须走 API，那么这个 API 通常不会真正好用。理想情况是核心自带的矿场、农场、设施和事件本身就是这个框架的第一批“使用者”。以后如果我们的“魔导兵团”想加入魔导能源节点，也应该和其他 Mod 作者一样，通过公开 Def 和 Worker 接口完成，而不是在框架内部加一个 `if (MagicArmyLoaded)`。

因此，开发过程中需要特别避免两种东西。第一种是把内容名称写进核心逻辑，例如 `if (outpost.type == Mining)`；第二种是为了未来可能存在的需求，一开始就创建十几个没有实际使用场景的抽象类。拓展性不是类越多越好，而是核心系统和具体内容之间的边界足够清楚。

当前开始开发时不需要一次实现完整的扩展 API。第一阶段只需要做一个真正能工作的最小据点：它可以存在于世界地图上，持有真实 Pawn，周期性根据 Mining 技能产生 Steel，并把 Steel 存在自己的库存里，存档和读档之后状态仍然正确。

但在实现这个最小版本时，需要避免做出明显堵死未来的决定。例如据点类型不应该用 enum；生产逻辑不要直接写进某个全局 Manager 的巨大 switch；运行状态和静态 Def 配置需要区分；特殊行为尽量不要通过不断继承 `WorldObject` 子类来实现。

每完成一个步骤，再单独讨论它在 RimWorld 中最合适的技术实现。比如真正开始“创建据点”时，再讨论应该继承哪个 WorldObject、是否需要 Caravan 到达后转换、是否需要自定义 Gizmo，以及对应的保存机制。开始做 Pawn 驻扎时，再确认 Pawn 应由什么容器持有以及怎样避免 World Pawn GC。开始做库存时，再讨论使用 `ThingOwner` 还是其他结构。

开发过程中不希望 Codex根据这份文档自行一次铺完整框架。它应该把这份文档当成长期设计约束。每次只处理当前要求的一个步骤，先说明准备使用哪些 RimWorld 类型、继承或组合什么对象、是否需要 Harmony Patch、状态保存在哪里、为什么选择这种方案，以及它对之后扩展有什么影响。确认方案之后再开始实现。

最终希望得到的不是“一次设计好所有东西”的框架，而是一个随着实际功能逐渐长出来、但始终保持明确扩展边界的据点系统。
