# 生产扩展

一条生产规则的完整定义拆成三块：

| 位置 | 类 | 内容 |
| --- | --- | --- |
| Def 静态配置 | `OutpostProductionProperties` | `id`、产物、周期、输入、tag、技能门槛、`workerClass` |
| 实例运行状态 | `OutpostProductionState` | 进度 `nextProductionTick`，以及这条规则特有的实例配置（农场种什么作物） |
| 行为 | `OutpostProductionWorker` | 计算、结算钩子、实例配置与 UI |

`OutpostFacilityDef.productions` 里每条规则配一个稳定的 `id`、产物 `product`、技能 `skill`、周期
`intervalTicks`，以及可选的 `workerClass`。周期默认 60000 tick（一天）。

`OutpostProductionWorker` 不是"几个计算函数的集合"，而是**这条规则的策略对象**：XML 给一条规则填一个
`workerClass`，框架懒加载出一个实例，**同一条 Def 规则在所有据点、所有设施实例之间共享这一个对象**。
所以：

- Worker 里绝不能存"某个据点选了什么"——那属于 `OutpostProductionState`（跨周期）或
  `OutpostProductionContext`（本次结算）。
- 计算钩子会被设施窗口的只读预测调用，绝不能改世界状态。
- 结算钩子只在真正结算时被调用，可以扣资源、生成物品、移人、发事件。

# 一、计算钩子（纯计算，UI 会用）

| 钩子 | 默认实现 |
| --- | --- |
| `CalculatePersonnelCapacity(pawns, outpostTypeDef, production)` | 汇总所有通过技能门槛的 Pawn 的 `capacityStat` |
| `CalculateOutput(capacity, outpostTypeDef, production[, state])` | `capacity × outputPerCapacity` |
| `CalculateProduction(...)` | 上面两个的组合入口，非 public virtual，结算与 UI 都走它 |
| `GetProduct(production, state)` | Def 上写死的 `product`；动态产物覆写它并把 `UsesDynamicProduct` 置 true |

`CalculateProduction` 统一拒绝 NaN / 无穷 / 负数产量并抛出带 `id` 的异常。产量修正（据点设施的
`productionModifiers`）发生在它返回之后，普通 Worker 不需要知道据点里有没有增产设施。

只改公式的例子（人员能力仍用默认实现）：

```csharp
using System;
using DreamsOutposts;

public class LogProductionWorker : OutpostProductionWorker
{
    public override float CalculateOutput(
        float personnelCapacity, OutpostTypeDef outpostTypeDef,
        OutpostProductionProperties production)
    {
        return (float)Math.Log(1d + Math.Max(0f, personnelCapacity));
    }
}
```

# 二、结算钩子（只在真正结算时走）

一个周期的固定顺序，每一步都是 `public virtual`：

```
OverrideProductionCycle          整轮接管？（默认 false，接管后框架不碰结算）
  ↓
CanProduce                       能不能生产（没电、被围困、声望不够…）  默认 true
  ↓
CalculateProduction              基础产量
  ↓
据点设施修正                       (base + Σoffset) × Πfactor   —— 框架做，不是钩子
  ↓
GenMath.RoundRandom              随机取整 → WantedAmount
  ↓
ModifyProduction                 最后调整（按据点状态改 WantedAmount）  默认空
  ↓
GetProduct                       产物 Def（取不到 → Failed，不扣材料）
  ↓
MaxProducibleAmount              本周期最多做几个                    默认 = 仓库输入上限
  ↓
ConsumeInputs                    真正扣输入（能量/声望/Pawn 在这里扣） 默认 = 扣仓库 inputs
  ↓
CreateProducts                   造物（非 Thing 产物在这里返回 null）  默认 = 按 stackLimit 造 Thing
  ↓
DeliverProducts                  产出送到哪（默认 = 据点库存）
  ↓
AfterProduction                  每轮恰好一次（finally），成功/空转/接管/异常都会调
                                 （异常路径先走 OnProductionFailed）
```

每个周期的上下文是公开的 `OutpostProductionContext`：

```csharp
public readonly Outpost Outpost;                     // 加钩子之前 Worker 连它都拿不到
public readonly OutpostFacility Facility;
public readonly OutpostProductionProperties Production;
public readonly OutpostProductionState State;
public readonly int Now;

public float BaseOutput;      // Worker 算出的基础产量（只读参考）
public float ModifiedOutput;  // 经据点设施修正后的浮点产量（只读参考）
public int WantedAmount;      // 取整后的目标产量（ModifyProduction 可改）
public int ActualAmount;      // 实际结算量（ConsumeInputs 可在扣料前调小）
public ThingDef Product;
public List<Thing> Products;

public int NextProductionInterval;   // 本次结算后 nextProductionTick 前进多少（默认 intervalTicks）
public OutpostProductionOutcome Outcome;  // Completed / Idle / Failed / TakenOver
public string FailureReason;
public bool Succeeded => Outcome == Completed;
```

它每结算一轮新建一个，不写存档。需要跨周期记住的东西（冷却、余量、累计值）必须写进 `State` 并自己存档。

## 结果分类

| `Outcome` | 含义 | 时间是否推进 |
| --- | --- | --- |
| `Completed` | 走完全部流程并投送完成（`ActualAmount > 0`） | 是 |
| `Idle` | 正常地没产出：判定不通过 / 取整为 0 / 资源不足一件都做不了 | 是，不报错 |
| `Failed` | 产物 Def 取不到 / 扣料失败 / 钩子抛异常，框架已写日志 | 是（钩子抛异常时还会中断本次检查的剩余补算并弹一次警告框） |
| `TakenOver` | 由 `OverrideProductionCycle` 自己处理完毕 | 是 |

## 几个典型需求怎么写

**产物进特定设施而不是据点库存**：覆写 `DeliverProducts`，自己决定去处。框架不再检查
`context.Products` 有没有被处理掉，**留下没有持有者的 Thing 就是泄漏**，所以要自己收尾
（放不下就销毁或回落到 `StoreInOutpostInventory`）。

**生产消耗能量、声望或 Pawn**：覆写 `MaxProducibleAmount`（回答"最多几个"）与 `ConsumeInputs`
（真正扣）。`ConsumeInputs` 可以在扣之前把 `context.ActualAmount` 调小，框架随后按调小后的值造物。
缺料时想把这一轮提前重试，就改 `context.NextProductionInterval`。

```csharp
public override int MaxProducibleAmount(OutpostProductionContext context)
{
    return context.Outpost.airdropPods;   // 举例：拿据点上现成的资源当上限
}

public override bool ConsumeInputs(OutpostProductionContext context)
{
    if (!base.ConsumeInputs(context))     // 先按默认扣仓库材料
        return false;

    context.Outpost.airdropPods -= context.ActualAmount;  // 再扣自己的资源
    return true;
}
```

**产物不是普通 Thing**（写进设施、生成 Pawn、给据点加数值）：覆写 `CreateProducts` 并返回 `null`，
框架就不会覆盖 `context.Products`；真正改变世界的动作放在那里或 `DeliverProducts` 里。

**生产完成时触发事件**：写 `AfterProduction`，用 `context.Outcome` / `Succeeded` 分辨结果。

**生产失败时产生副作用**：异常类失败写 `OnProductionFailed(context, ex)`；材料不足这类正常空转不会走
那里，要在 `AfterProduction` 里看 `Outcome == Idle`。
（这两个回调自己抛出的异常只会记一条日志，不会影响结算结果与计时推进。）

**按据点状态改变产量**：写 `ModifyProduction`，此时 `context` 里有 `Outpost` / `Facility` / `State`，
直接改 `context.WantedAmount`。

**整轮接管**：`OverrideProductionCycle` 返回 true，框架不再调用 `CalculateProduction`、不限额、不扣料、
不造物、不投送，只负责按 `NextProductionInterval` 推进计时并在 `AfterProduction` 里通知你。
适合"完全不复用公式与库存"的规则；只改一两步的话，覆写对应的细粒度钩子更合适。

## 可直接复用的公开工具

| 方法 | 用途 |
| --- | --- |
| `OutpostProductionUtility.TakeInputsFromStock(outpost, facility, production, amount)` | 默认的扣仓库输入，可在覆写 `ConsumeInputs` 时先调它 |
| `OutpostProductionUtility.MakeProductThings(product, amount)` | 按 stackLimit 拆堆造 Thing |
| `OutpostProductionUtility.StoreInOutpostInventory(outpost, products)` | 把产出存进据点仓库 |
| `OutpostProductionUtility.ApplyModifiers(outpost, production, baseOutput)` | 单独套用据点设施的产量修正 |
| `OutpostProductionUtility.RuleLabel(facility, production)` | 与框架报错一致的规则标识 `设施Def.规则id` |

# 三、实例配置钩子（每个设施实例各自的选择）

"这条规则要不要玩家配置、配置长什么样"完全由 Worker 决定，窗口里没有任何 `if (production is 农业)`
之类的硬编码：窗口只问 `HasConfiguration`，然后无条件把那一行交给 `DrawConfiguration`。
养殖选动物、训练选技能、化工厂选配方都只需覆写这四个方法，并用 `StateClass` 指向自己的状态子类。

| 钩子 | 默认 |
| --- | --- |
| `HasConfiguration(production)` | false（不需要配置） |
| `EnsureConfiguration(production, state)` | 空（新建/读档/改 XML 后对齐选择） |
| `DrawConfiguration(rect, label, production, state)` | 空 |
| `ConfigurationSummary(production, state)` | null |

多态靠 XML 的 `Class` 属性（`DirectXmlToObject.ClassTypeOf` 会实例化成指定子类，字段查找沿继承链向上）：

```xml
<productions>
  <li Class="DreamsOutposts.OutpostProductionProperties_Farming">
    <id>crops</id>
    <sowTag>Ground</sowTag>
  </li>
</productions>
```

子类的构造函数负责绑好自己的 `workerClass`，XML 里通常不必再写。**不要**为了"通用"在基类加
`List<Def> allowedChoices` 这类万能字段：特殊 Worker 配特殊 Properties 本身就是扩展点。

# 四、已知边界

- **设施窗口的"预期产出 / 进度条"不调用结算钩子**（它们每帧在 OnGUI 里跑，钩子允许改世界状态）。
  UI 显示的是 `CalculateProduction + ApplyModifiers` 的结果，不含 `CanProduce` / `ModifyProduction`
  的修正；进度条按 Def 的 `intervalTicks` 估算，所以用 `NextProductionInterval` 动态改周期的规则，
  进度条只是近似。
- 调度骨架（250 tick 检查一次、按绝对 tick 补算、单次检查最多补算 `MaxCatchUpCyclesPerCheck` 轮、
  异常隔离、错误日志与警告框）对任何 Worker 都一样，不通过钩子开放；要改的是结算本身，用
  `OverrideProductionCycle`。
