我：
那怎么让别人接入会更加有拓展性
不是别人不用引用程序集，而是降低别人工作量
===
ChatGPT：
对，我理解你的意思了：**不是追求“零 C#”，而是让别人接你的框架时，20 分钟能做出东西，而不是先研究你源码两天。**

这个目标会直接改变 API 怎么设计。我建议遵循一句话：

> **常见联动只填数据；特殊联动才写逻辑。**

RimWorld 本身的 Def / `DefModExtension` 很适合这么干：Def 可以承载静态配置，也允许 Mod 给已有 Def 挂扩展数据。([GitHub][1])

### 最关键的一层：把“能力”抽象出来

你现在不要把代码写成：

```csharp
if (outpost.HasHospital)
    ...
if (outpost.HasArmory)
    ...
if (outpost.HasMagicGenerator)
    ...
```

以后别人接入一个 Mod，你就得改核心。

应该变成：

```text
据点拥有若干 Capability / Stat

Defense
Medical
Research
Production
Storage
Transport
Social
...
```

设施、Pawn、装备、其他 Mod 内容都只是给这些数值提供贡献。

例如：

```text
自动炮塔
Defense +80

医院
Medical +60

高级实验室
Research +100
```

第三方作者想给自己的 Mod 加：

> 魔导维修核心

只需要定义：

```xml
<OutpostFacilityDef>
    <defName>ArcaneRepairCore</defName>

    <statOffsets>
        <Medical>40</Medical>
        <Defense>15</Defense>
    </statOffsets>
</OutpostFacilityDef>
```

你的 UI、事件系统、存档、设施槽、能力计算**全部自动认识它**。

这就是第一层真正能降低工作量的地方。

---

### 事件系统尤其应该做成“乐高”

比如我们刚才那个：

> 一伙难民来到据点。

“俘虏他们”这个选项千万不要专门写一个 `RefugeeCaptureEvent.cs`。

而应该有通用组件：

```text
Requirement:
StatAtLeast
HasFacility
HasItem
PawnSkillAtLeast
OutpostLevelAtLeast
ResearchFinished

Effect:
AddItem
RemoveItem
AddPawn
AddPrisoner
AddSlave
DamageFacility
ChangeProduction
TriggerRaid
ChangeGoodwill
AddCondition
```

于是事件本身只是：

```xml
<option>
    <label>俘虏他们</label>

    <requirements>
        <li Class="StatAtLeast">
            <stat>Defense</stat>
            <value>120</value>
        </li>
    </requirements>

    <effects>
        <li Class="AddPawn">
            <status>Slave</status>
            <count>2~4</count>
        </li>

        <li Class="ChangeGoodwill">
            <amount>-15</amount>
        </li>
    </effects>
</option>
```

以后第三方 Mod 想写：

> “发现三只来自 Alpha Animals 的特殊生物。”

如果你的内置 Effect 已经足够，他甚至**完全不用 C#**。

写一个 XML 就结束。

这不是为了排斥程序集，而是因为这是最快的接入方式。

---

### 但一定要给高级作者留 C# Worker

否则迟早有人想做：

> “我的设施根据月相、天气、Pawn 的灵能敏感度和库存里的猫数量计算产量。”

😂 这时候纯 XML 救不了了。

所以每个关键模块允许：

```csharp
workerClass
```

比如：

```xml
<OutpostFacilityDef>
    <defName>WeirdFacility</defName>
    <workerClass>SomeMod.MyFacilityWorker</workerClass>
</OutpostFacilityDef>
```

他们引用你的 DLL，然后：

```csharp
public class MyFacilityWorker : OutpostFacilityWorker
{
    public override float GetProductionModifier(Outpost outpost)
    {
        return ...;
    }
}
```

也就是说形成三级接入：

**简单内容 → XML**

**已有游戏对象联动 → DefModExtension / XML Patch**

**真正特殊机制 → 继承 Worker / 实现接口**

而不是所有作者一上来都必须实现五个 interface。

---

### 武器防御就是一个特别好的“自动兼容”案例

这里甚至不要让第三方作者做任何适配。

你的核心直接认识所有 `ThingDef` 武器：

```text
是武器？
↓
读取伤害、射速、射程……
↓
自动计算 Outpost Defense
```

于是某个 Mod 新加了：

> Plasma Annihilator Mk.V

你根本不知道这个 Mod 存在。

玩家把它送进据点：

> Plasma Annihilator Mk.V
> 防御贡献：143

**自动兼容。**

只有这个 Mod 作者觉得：

> “不行，我这个武器是特殊轨道武器，你的算法低估了。”

他才需要：

```xml
<modExtensions>
    <li Class="YourMod.OutpostWeaponExtension">
        <defenseMultiplier>2.0</defenseMultiplier>
    </li>
</modExtensions>
```

这种 API 非常舒服：

> **默认什么都不用做；想精调的时候才接入。**

RimWorld 的 `DefModExtension` 本来就允许这样给现有 Def 挂自定义静态信息。([GitHub][1])

---

## 我甚至建议你专门做一个 `OutpostStatDef`

不要把能力写死成 enum：

```csharp
enum OutpostStat
{
    Defense,
    Medical,
    Research
}
```

**千万别这么干。**

因为别人以后想加入：

> MagicPower
> PsychicControl
> GeneticResearch
> AnomalyContainment

就没法扩展。

应该：

```xml
<OutpostStatDef>
    <defName>Defense</defName>
    <label>防御能力</label>
</OutpostStatDef>
```

第三方直接：

```xml
<OutpostStatDef>
    <defName>ArcanePower</defName>
    <label>魔导能量</label>
</OutpostStatDef>
```

然后他的事件：

```text
需要 ArcanePower ≥ 100
```

他的设施：

```text
ArcanePower +40
```

他的其他设施：

```text
ArcanePower ×1.2
```

**你完全不用更新核心。**

这个设计我认为特别重要。

---

### 据点类型也同理

不要写：

```csharp
switch (outpost.type)
{
    case Mining:
    case Farming:
}
```

而是：

```xml
<OutpostTypeDef>
    <defName>MiningOutpost</defName>
    <label>采矿据点</label>

    <productionWorker>...</productionWorker>

    <allowedFacilityTags>
        <li>Mining</li>
        <li>General</li>
    </allowedFacilityTags>
</OutpostTypeDef>
```

那么别人加一个：

> 基因培育据点

基本只需要：

```text
一个 OutpostTypeDef
几种 FacilityDef
几条 EventDef
```

你的框架负责：

**建造、升级、Pawn 驻扎、库存、空投、防御、事件 UI、存档。**

这才叫真正降低工作量。

---

## 最终我建议你的公开扩展面控制在大约这几个东西

| 类型                     | 第三方拿它干什么             |
| ---------------------- | -------------------- |
| `OutpostTypeDef`       | 新据点类型                |
| `OutpostFacilityDef`   | 新设施                  |
| `OutpostStatDef`       | 新能力值                 |
| `OutpostEventDef`      | 新事件/事件链              |
| `OutpostProductionDef` | 新生产方式                |
| `DefModExtension`      | 修改现有武器/Pawn/物品对据点的作用 |
| `Worker`               | 实在表达不了的特殊 C# 行为      |

这样已经非常强了。

而且我特别建议你给第三方作者附一个 **ExampleIntegration**：

```text
ExampleIntegration/
├─ About/
├─ Defs/
│  ├─ Outposts.xml
│  ├─ Facilities.xml
│  └─ Events.xml
└─ Source/
   └─ ExampleWorker.cs
```

里面就展示：

> 5 分钟添加一个设施
> 10 分钟添加一个事件
> 15 分钟添加一种据点
> 如何写一个特殊 Worker

再配一个 DevMode：

**Create Test Outpost / Add Facility / Trigger Event / Recalculate Stats / Dump Stat Breakdown**

第三方作者会非常舒服。

所以现在你写第一版的时候，我建议**不用先把整个 Mod API 实现完**，但有两个决定现在就要做：

**`OutpostStat` 不写死成 enum；设施和事件不写死成具体 C# 类。**

这两个坑只要避开，之后扩展性基本就有根了。至于完整第三方 API，等你的矿业据点 MVP 跑起来再抽象都来得及。

[1]: https://github.com/Dev-Jahn/rimworld-modding-reference/blob/main/01-rimworld-core/def-system.md?utm_source=chatgpt.com "rimworld-modding-reference/01-rimworld-core/def-system.md at main · Dev-Jahn/rimworld-modding-reference · GitHub"

===
我：我自己的worker预期是什么样的
===
对。这里的 `Worker` 我预期不是“一个据点对应一个 C# 类”，而更像 RimWorld 里常见的 **策略对象**：

> **Def 决定“它是什么”**
> **Outpost 实例保存“它现在是什么状态”**
> **Worker 决定“它特殊情况下怎么算、怎么行动”**

这样最好。

VOE 现在很多逻辑实际上是通过具体 `Outpost` 子类 override 来做的，例如 `Outpost_Encampment` 直接 override `Tick()` 来处理其中 Pawn 的食物、休息、治疗；底层 `Outpost` 本身也提供了 `TotalSkill()`、`IsCapable()` 等虚方法。([GitHub][1]) 你可以借鉴“可覆写行为”的思想，但我不建议继续走“一种据点一个 Outpost 子类”的路线。

## 我预期你的三个东西长这样

### `OutpostDef`

纯配置。

```csharp
public class OutpostDef : Def
{
    public Type workerClass = typeof(OutpostWorker);

    public List<OutpostProductionDef> productions;

    public int baseStorage;
    public int maxLevel;

    public List<string> facilityTags;

    [Unsaved]
    private OutpostWorker worker;

    public OutpostWorker Worker
    {
        get
        {
            worker ??= (OutpostWorker)Activator.CreateInstance(workerClass);
            worker.def = this;
            return worker;
        }
    }
}
```

XML：

```xml
<MyMod.OutpostDef>
    <defName>MiningOutpost</defName>
    <label>mining outpost</label>

    <baseStorage>1000</baseStorage>
    <maxLevel>3</maxLevel>

    <workerClass>MyMod.OutpostWorker</workerClass>
</MyMod.OutpostDef>
```

注意这里：

**普通矿场甚至直接使用默认 `OutpostWorker`。**

根本不用单独写 `MiningOutpostWorker`。

---

## `Outpost`

这是一个**有存档状态的实体**。

它保存：

```csharp
public class Outpost : WorldObject
{
    public OutpostDef OutpostDef;

    public int level = 1;

    public List<Pawn> pawns = new();
    public ThingOwner inventory;

    public List<OutpostFacility> facilities = new();

    public int lastProductionTick;

    public OutpostWorker Worker => OutpostDef.Worker;
}
```

以后还有：

```text
当前事件
设施损坏
累计运营时间
升级进度
库存
Pawn
```

这些东西都应该放 `Outpost`。

**绝对不要放 Worker。**

因为 Worker 通常是 Def 共用的。

比如整个游戏里有 15 个 Mining Outpost：

```text
MiningOutpost Def
        ↓
同一个 OutpostWorker

矿场 A ┐
矿场 B ├→ 调 Worker 算
矿场 C ┘
```

所以 Worker 必须尽量 **stateless**。

这点以后会救你的命。

---

# 那 `OutpostWorker` 到底干什么？

我第一版甚至只给它很少几个接口：

```csharp
public class OutpostWorker
{
    public OutpostDef def;

    public virtual bool CanCreate(
        IEnumerable<Pawn> pawns,
        int tile,
        out string reason)
    {
        reason = null;
        return true;
    }

    public virtual void Produce(
        Outpost outpost,
        int ticks)
    {
    }

    public virtual void ModifyStat(
        Outpost outpost,
        OutpostStatDef stat,
        ref float value)
    {
    }

    public virtual void OnCreated(Outpost outpost)
    {
    }

    public virtual void OnInterval(Outpost outpost)
    {
    }
}
```

甚至还可以更少。

重点不是把所有东西都塞进去，而是：

> **当 XML 表达不了的时候，Worker 给我逃生口。**

---

# 举个例子就非常明显了

普通采矿据点：

> Mining 技能 → 钢铁产量。

这种东西完全可以由 XML 定义：

```xml
<production>
    <thingDef>Steel</thingDef>
    <skill>Mining</skill>
    <amountPerSkill>0.8</amountPerSkill>
</production>
```

默认 Worker 自动处理。

所以：

```xml
<workerClass>MyMod.OutpostWorker</workerClass>
```

甚至可以不填。

---

但第三方作者突然想做：

## “捕鲸基地”

规则：

> 只有靠海才能建。
> 产量取决于所在 Tile 是否邻接海洋。
> 冬季鲸油产量提高。
> 每隔一段时间可能捕获特殊鲸类。

这种 XML 写起来就开始抽象爆炸了。

那他写：

```xml
<workerClass>
    WhaleHuntingMod.WhalingOutpostWorker
</workerClass>
```

然后：

```csharp
public class WhalingOutpostWorker : OutpostWorker
{
    public override bool CanCreate(
        IEnumerable<Pawn> pawns,
        int tile,
        out string reason)
    {
        if (!WorldUtility.IsCoastal(tile))
        {
            reason = "Must be built on the coast.";
            return false;
        }

        reason = null;
        return true;
    }

    public override void Produce(Outpost outpost, int ticks)
    {
        float amount = CalculateWhaleOil(outpost, ticks);

        outpost.Inventory.Add(
            ThingMaker.MakeThing(MyDefOf.WhaleOil),
            (int)amount
        );
    }
}
```

**这就是 Worker 真正存在的意义。**

不是：

> 所有据点逻辑都写 Worker。

而是：

> 默认系统解决 90%，Worker 解决剩下奇怪的 10%。

---

# 设施 Worker 也一样

这个我觉得以后你肯定需要。

正常设施：

```xml
<OutpostFacilityDef>
    <defName>Fortification</defName>

    <statOffsets>
        <Defense>100</Defense>
    </statOffsets>
</OutpostFacilityDef>
```

根本不需要 C#。

但有人做：

> “血肉培育池：每有一个奴隶，生物产量 +8%。”

这就不是普通 `statOffsets` 能表达的。

于是：

```csharp
public class FacilityWorker
{
    public OutpostFacilityDef def;

    public virtual void ModifyStat(
        Outpost outpost,
        OutpostFacility facility,
        OutpostStatDef stat,
        ref float value)
    {
    }

    public virtual void OnInterval(
        Outpost outpost,
        OutpostFacility facility)
    {
    }
}
```

然后他的：

```csharp
public class FleshVatWorker : FacilityWorker
{
    public override void ModifyStat(
        Outpost outpost,
        OutpostFacility facility,
        OutpostStatDef stat,
        ref float value)
    {
        if (stat == MyDefOf.BiologicalProduction)
        {
            int slaves = outpost.Pawns
                .Count(p => p.IsSlave);

            value *= 1f + slaves * 0.08f;
        }
    }
}
```

完成。

---

# 事件反而不要全部使用一个 `EventWorker`

这里我会拆。

因为你的 Frostpunk 式事件：

> 难民出现。

里面其实有两个完全不同的问题：

**这个选项能不能选？**

和：

**选完发生什么？**

所以最好是：

```csharp
OutpostEventRequirement
```

以及：

```csharp
OutpostEventEffect
```

例如：

```xml
<option>
    <label>俘虏他们</label>

    <requirements>
        <li Class="MyMod.Requirement_Stat">
            <stat>Defense</stat>
            <min>150</min>
        </li>
    </requirements>

    <effects>
        <li Class="MyMod.Effect_AddSlaves">
            <count>3</count>
        </li>
    </effects>
</option>
```

第三方作者需要特别奇怪的判断：

> 必须有一个拥有某种基因、且 Social > 12 的奴隶。

直接：

```csharp
public class MyWeirdRequirement
    : OutpostEventRequirement
{
    public override bool Met(Outpost outpost)
    {
        ...
    }
}
```

这就行。

---

# 所以你的 Worker 层最终我预期非常薄

大概就是这一套：

| Worker             | 干嘛          |
| ------------------ | ----------- |
| `OutpostWorker`    | 整个据点类型的特殊规则 |
| `FacilityWorker`   | 某设施的特殊行为    |
| `ProductionWorker` | 非常特殊的生产公式   |
| `EventRequirement` | 特殊事件条件      |
| `EventEffect`      | 特殊事件结果      |

但**前三个甚至第一版都不用全做**。

你现在真正需要的只有：

```text
OutpostDef
Outpost
OutpostWorker
```

先把矿场做起来。

等做到设施，再抽 `FacilityWorker`。

做到事件，再抽 `Requirement / Effect`。

不要现在一次把 API 宇宙造完。

---

还有一个设计原则我特别建议你坚持：

**第三方作者继承 Worker 时，不应该需要重新实现整个据点。**

例如他只是想修改产量，就应该：

```csharp
public override void Produce(...)
```

结束。

而不是：

```csharp
Tick()
ExposeData()
GetGizmos()
Draw()
Notify_CaravanArrived()
CalculateDefense()
CalculateStorage()
...
```

全部自己负责。

这其实是我觉得 VOE 那类直接 subclass `Outpost` 的结构，你可以明显超越的地方：**核心状态和通用机制由你的 Framework 掌管，Worker 只负责差异。** VOE 当前确实大量依靠 `Outpost` 子类 override 特定行为，因此你做新框架时有机会把“内容”和“世界对象生命周期”分得更干净。([GitHub][1])

所以如果你现在准备真正开写，我会把第一版结构压到非常小：

```text
OutpostDef.cs
Outpost.cs
OutpostWorker.cs
OutpostManager.cs

Defs/
    MiningOutpost.xml
```

**先做到 `MiningOutpost.xml` 不写一行专用 C# 就能正常生产钢铁。**

如果这一点成立，你这个扩展框架的方向基本就走对了。

[1]: https://github.com/Vanilla-Expanded/VanillaOutpostsExpanded/blob/main/1.6/Source/VOE/Outpost_Encampment.cs?utm_source=chatgpt.com "VanillaOutpostsExpanded/1.6/Source/VOE/Outpost_Encampment.cs at main · Vanilla-Expanded/VanillaOutpostsExpanded · GitHub"
